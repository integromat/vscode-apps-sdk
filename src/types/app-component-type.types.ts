/**
 * Note: The type `app` is used for the app itself, not for a component of the app.
 *       It contains the base definition of the app, readme, common data.
 */
export type AppComponentType = 'app' | 'connection' | 'webhook' | 'module' | 'rpc' | 'function';
