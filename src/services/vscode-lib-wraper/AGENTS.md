# Directory `vscode-lib-wraper` documentation

This directory contains the wrapper around the VSCode API.

The purpose is to provide:

- actual VSCode API if app is used as VSCode extension,
- own implementation with same interface if app is used as CLI.
