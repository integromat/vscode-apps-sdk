Directory `cli` documentation
=============================

This directory implements the Custom Apps Local Development CLI tool.

CLI is a command-line wrapper around the local development library placed in `src/local-development` directory.
It allows users to perform local development tasks without using the VSCode extension.
