######
# This file is autogenerated from https://github.com/integromat/mono/blob/master/libs/github-resources/src/repositories/vscode-apps-sdk.ts. Do not edit manually!!!
######

name: PR
on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - labeled
      - unlabeled
      - edited
  merge_group:

# run only latest workflow, cancel other runs of this workflow
concurrency:
  group: validate-pr-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_COLOR: '1'

jobs:
  validate-pr:
    name: Validate PR
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      issues: write
      pull-requests: write
      contents: read
    env:
      NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      # Some repositories commit .npmrc files that read NPM_TOKEN; project .npmrc
      # entries override the setup-node user-level config that reads NODE_AUTH_TOKEN.
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        if: github.event_name != 'merge_group'

      - name: Setup Node.js
        if: github.event_name != 'merge_group'
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          registry-url: https://registry.npmjs.org
          node-version: '24.x'

      - name: Validate PR
        if: github.event_name != 'merge_group'
        run: npx @integromat/actions-toolkit validate-pr --skipPrDescriptionLengthValidation
